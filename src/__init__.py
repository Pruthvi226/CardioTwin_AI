"""CardioTwin AI package.

The original repository contains several research scripts at the top level and
under ``src``.  The demo-ready implementation lives in structured subpackages
so the project has stable import paths for scripts, tests, the API, and the
Streamlit dashboard.
"""

